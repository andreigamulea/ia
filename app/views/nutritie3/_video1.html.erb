<% border_style = (defined?(previous_video_tip) && previous_video_tip == 'nutritie2' && video.tip == 'nutritie3') ? 'border-top: 3px solid #000;' : '' %>

<tr style="<%= border_style %>">
<% if video.tip == 'nutritie3' || video.tip == 'nutritie2'%>
<td>
  <% if video.ordine > 1000 && video.ordine < 2000%>
    <%= video.ordine - 1000 %>
  <%elsif video.ordine > 2000 && video.ordine < 3000%>  
  <%= video.luna %>
  <%elsif video.ordine > 3000 && video.ordine < 4000%>  
  <%= video.luna %>
  <%elsif video.ordine > 4000 && video.ordine < 5000%>  
  <%= video.ordine - 4000 %>
  <% else %>
    <%= video.ordine %>
  <% end %>
</td>
<% end %>
  <td><%= video.nume %></td>
  <td><%= video.descriere %></td>
 
  <td>   
    <%= link_to "Vezi video", myvideo2_path(id: video.id, link: video.link), class: "btn btn-primary" %>
  </td>
  <% if defined?(previous_video_tip) %>
  <td>
  <%= link_to "Descarcă PDF", video.linkpdf, class: "btn btn-primary", download: '' %>


  </td>
  <%end%>
</tr>
