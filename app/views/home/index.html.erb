<section class="home">

<div class="nutritie">
   <div class="row">
      <div class="column">
         <a href="/valori-nutritionale">
         <img src="<%= image_path('nutritie-ayushcell.jpg') %>" alt="Imagine">
         </a>
      </div>
      <div class="column">
         <h1>Aplicație Nutriție</h1>
         <div class="description">Descoperă conținutul nutritiv al alimentelor și calculează numărul de calorii, proteine, lipide, carbohidrați și fibre la fiecare masă! Alege în fiecare zi o alimentație sănătoasă și echilibrată.</div>
         <div class="button">
            <% if (current_user && current_user.listacursuri.any? { |curs| curs.nume == "Nutritie" }) || (current_user && current_user.role == 1) %>
            <a href="/valori-nutritionale">Intră în aplicație</a>
            <% elsif current_user %>   
            <%= link_to 'Intră în aplicație', servicii_path, class: 'button-link' %>
            <% else %>   
            <%= link_to 'Intră în aplicație', new_user_session_with_return_path('vn'), class: 'button-link' %>
            <% end %>
         </div>
      </div>
   </div>
</div>
<div class="nutritie endrow">
   <div class="row">
      <div class="column ivl">
         <h1>Lista ingrediente vegetale</h1>
         <div class="description">Accesează lista ingredientelor vegetale ce pot fi utilizate in suplimentele alimentare.</div>
         <div class="button">
            <% if (current_user && current_user.listacursuri.any? { |curs| curs.nume == "Lista vegetale" }) || (current_user && current_user.role == 1) %>
            <a href="/lista_vegetales">Intră în aplicație</a>
            <% elsif current_user %>   
            <%= link_to 'Intră în aplicație', servicii_path, class: 'button-link' %>
            <% else %>   
            <%= link_to 'Intră în aplicație', new_user_session_with_return_path('lg'), class: 'button-link' %>
            <% end %>
         </div>

      </div>
      <div class="column ivl1">
      <a href="/lista_vegetales">
      <img src="<%= image_path('listavegetale.jpg') %>" alt="Imagine"></a>
   </div>
   </div>
</div>


</section>