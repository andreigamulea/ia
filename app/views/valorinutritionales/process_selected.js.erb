<% if @selected_values.any? %>
  var selectedValues = <%= @selected_values.to_json %>;

  // Aici puteți procesa și utiliza datele cum doriți.
  // De exemplu, puteți face un request AJAX cu valorile selectate și apoi să modificați DOM-ul paginii.

<% end %>