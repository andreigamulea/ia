
<section class="tabel-container rotate-landscape">
    <h1 class="alimente-nutritionale">Calculator Valori Nutritionale</h1>

    <div class="table-responsive">
        <table class="table table-striped table-bordered table-hover">
            <thead class="thead-dark">
                <tr class= "trdesk">
                    <th style="text-align: left; height: 50px; vertical-align:middle; width: 30%;">Servicii</th>                    
                    <th style="text-align: left; height: 50px; vertical-align:middle; width: 30%;">Detalii</th>
                    <th style="text-align: left; height: 50px; vertical-align:middle; width: 14%;">Pret (fara tva)</th>
                    <th style="text-align: left; height: 50px; vertical-align:middle; width: 14%;">Pret (cu tva)</th>
                    <th style="text-align: left; height: 50px; vertical-align:middle; width: 12%;">Actiune</th>
                </tr>      
            </thead>
            <tbody>
            <% @prods.select{ |prod| prod.curslegatura == 'Nutritie' }.each do |prod| %>
                    <tr>
                        <td> <%= prod.nume %> </td>                        
                        <td><%= prod.detalii %></td> 
                        <td><%= (prod.pret/(1 + TVA / 100.0)).round(2) %> lei</td>
                         <td><%= prod.pret.to_i %> lei</td>

                        <td> <%= link_to 'Comanda', datefacturare_path(s: prod.id), class: 'btn btn-primary' %> </td>      
                    </tr>  
                <% end %>  
            </tbody>
        </table>
    </div>
    <h1 class="alimente-nutritionale">Consultare Selectiva Lista Ingrediente Vegetale</h1>

    <div class="table-responsive">
        <table class="table table-striped table-bordered table-hover">
            <thead class="thead-dark">
                <tr class= "trdesk">
                    <th  style="text-align: left; height: 50px; vertical-align:middle; width: 30%;">Servicii</th>                    
                    <th  style="text-align: left; height: 50px; vertical-align:middle; width: 30%;">Detalii</th>
                    <th  style="text-align: left; height: 50px; vertical-align:middle; width: 14%;">Pret (fara tva)</th>
                    <th  style="text-align: left; height: 50px; vertical-align:middle; width: 14%;">Pret (cu tva)</th>
                    <th  style="text-align: left; height: 50px; vertical-align:middle; width: 12%;">Actiune</th>
                </tr>      
            </thead>
            <!-- Primul tbody pentru produsele cu pret = 0 -->
            <tbody>
            <% @prods.select{ |prod| prod.curslegatura == 'Lista vegetale' && prod.pret == 0 }.each do |prod| %>
                <tr>
                  <td> <%= raw prod.nume.gsub('promo acces 24h', '<span class="text-danger">promo acces 24h</span>') %> </td>
                                   
                <td><%= prod.detalii %></td> 
                <td><%= (prod.pret/(1 + TVA / 100.0)).round(2) %> lei</td>
                <td><%= prod.pret.to_i %> lei</td>
                <td>
                <% if (current_user && current_user.cursuri.any? { |curs| curs.listacursuri.nume == "Lista vegetale" && (curs.datasfarsit.nil? || Date.current <= curs.datasfarsit) }) || (current_user && current_user.role == 1) %>


                <%= link_to 'Comanda', listavegetalegratis_home_path(acces: "da"), class: 'btn btn-primary' %>


                    <% elsif current_user %>   
                    <%= link_to 'Comanda', listavegetalegratis_home_path(acces: "nu"), class: 'btn btn-primary' %>

                    <% else %>   
                    <%= link_to 'Comanda', new_user_session_with_return_path('lg'), class: 'btn btn-primary' %>
                    <% end %>
                </td>      
                </tr>
                <% end %>
            </tbody>
            <tbody>
            <% @prods.select{ |prod| prod.curslegatura == 'Lista vegetale' && prod.pret > 0 }.each do |prod| %>
                    <tr>
                        <td> <%= prod.nume %> </td>                        
                        <td><%= prod.detalii %></td> 
                        <td><%= (prod.pret/(1 + TVA / 100.0)).round(2) %> lei</td>
                         <td><%= prod.pret.to_i %> lei</td>

                        <td> <%= link_to 'Comanda', datefacturare_path(s: prod.id), class: 'btn btn-primary' %> </td>      
                    </tr>  
                <% end %>  
            </tbody>
        </table>
    </div>

    <h1 class="alimente-nutritionale">Acces prezentari video din Tabara de ayurveda 2023 etapa de vara  partea a 2-a </h1>

    <div class="table-responsive">
        <table class="table table-striped table-bordered table-hover">
            <thead class="thead-dark">
                <tr class= "trdesk">
                    <th style="text-align: left; height: 50px; vertical-align:middle; width: 30%;">Servicii</th>                    
                    <th style="text-align: left; height: 50px; vertical-align:middle; width: 30%;">Detalii</th>
                    <th style="text-align: left; height: 50px; vertical-align:middle; width: 14%;">Pret (fara tva)</th>
                    <th style="text-align: left; height: 50px; vertical-align:middle; width: 14%;">Pret (cu tva)</th>
                    <th style="text-align: left; height: 50px; vertical-align:middle; width: 12%;">Actiune</th>
                </tr>      
            </thead>
            <tbody>
            <% @prods.select{ |prod| prod.curslegatura == 'tayv2' }.each do |prod| %>
                    <tr>
                    <td>
                        <%= raw prod.nume.gsub('pentru participanti deja inscrisi', '<span class="text-danger">pentru participanti deja inscrisi</span>').gsub('cu inscriere', '<span class="text-danger">cu inscriere</span>') %>
                    </td>                      
                        <td><%= prod.detalii %></td> 
                        <td><%= (prod.pret/(1 + TVA / 100.0)).round(2) %> lei</td>
                         <td><%= prod.pret.to_i %> lei</td>

                        <td> <%= link_to 'Comanda', datefacturare_path(s: prod.id), class: 'btn btn-primary' %> </td>      
                    </tr>  
                <% end %>  
            </tbody>
        </table>
    </div>


    <% if @prods.any? { |prod| prod.id == 11 } && @prods.any? { |prod| prod.id == 12 } %>  
    <a name="nutritie-modul3"></a>
    <h1 class="alimente-nutritionale">Acces Curs Nutritie Modulul 3 </h1>

    <div class="table-responsive">
        <table class="table table-striped table-bordered table-hover">
            <thead class="thead-dark">
                <tr class= "trdesk">
                    <th style="text-align: left; height: 50px; vertical-align:middle; width: 30%;">Servicii</th>                    
                    <th style="text-align: left; height: 50px; vertical-align:middle; width: 30%;">Detalii</th>
                    <th style="text-align: left; height: 50px; vertical-align:middle; width: 14%;">Pret (fara tva)</th>
                    <th style="text-align: left; height: 50px; vertical-align:middle; width: 14%;">Pret (cu tva)</th>
                    <th style="text-align: left; height: 50px; vertical-align:middle; width: 12%;">Actiune</th>
                </tr>      
            </thead>
            <tbody>
            <% @prods.select{ |prod| prod.curslegatura == 'Nutritie3' }.each do |prod| %>
                    <tr>
                    <td>
                        <%= raw prod.nume.gsub('pentru participanti deja inscrisi', '<span class="text-danger">pentru participanti deja inscrisi</span>').gsub('cu inscriere', '<span class="text-danger">cu inscriere</span>') %>
                    </td>                      
                        <td><%= prod.detalii %></td> 
                        <td><%= (prod.pret/(1 + TVA / 100.0)).round(2) %> lei</td>
                         <td><%= prod.pret.to_i %> lei</td>

                        <td> <%= link_to 'Comanda', datefacturare_path(s: prod.id), class: 'btn btn-primary' %> </td>      
                    </tr>  
                <% end %>  
            </tbody>
        </table>
    </div>

<%end%>











</section>


