<%= line_chart @visits_per_day %>

<h3>Accesari din:</h3> <% @os_stats.each do |os, count| %>
    <p><%= os %>: <%= count %></p>
  <% end %>
  <h3>Dispozitive de unde s-a realizat accesarea:</h3> <% @device_stats.each do |device_type, count| %>
    <p><%= device_type %>: <%= count %></p>
  <% end %>
